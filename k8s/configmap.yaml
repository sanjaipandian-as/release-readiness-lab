apiVersion: v1
kind: ConfigMap
metadata:
  name: checkout-api-config
  namespace: checkout-system
data:
  LOG_LEVEL: "info"
  PORT: "5000"
  # Note: Sensitive variables (DATABASE_URL, REDIS_URL) are managed via checkout-api-secrets in secret.yaml to comply with SRE security guidelines.
