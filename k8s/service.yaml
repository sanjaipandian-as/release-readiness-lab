apiVersion: v1
kind: Service
metadata:
  name: checkout-api-service
  namespace: checkout-system
  labels:
    app: checkout-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: checkout-api
